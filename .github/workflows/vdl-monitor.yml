name: VDL Monitor

on:
  workflow_dispatch:
  schedule:
    - cron: "24 8 * * *"
      timezone: "Europe/Paris"

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run VDL monitor
        env:
          SITEMAP_TIMEOUT: ${{ vars.SITEMAP_TIMEOUT || '25' }}
          SITEMAP_CONNECT_TIMEOUT: ${{ vars.SITEMAP_CONNECT_TIMEOUT || '8' }}
          PAGE_TIMEOUT: ${{ vars.PAGE_TIMEOUT || '20' }}
          HTTP_RETRIES: ${{ vars.HTTP_RETRIES || '3' }}
          SITEMAP_WORKERS: ${{ vars.SITEMAP_WORKERS || '20' }}
          MAX_SITEMAP_DOCUMENTS: ${{ vars.MAX_SITEMAP_DOCUMENTS || '150' }}
          MAX_SITEMAP_SECONDS_PER_SITE: ${{ vars.MAX_SITEMAP_SECONDS_PER_SITE || '90' }}
          SECOND_PASS_SITEMAP_TIMEOUT: ${{ vars.SECOND_PASS_SITEMAP_TIMEOUT || '40' }}
          SECOND_PASS_SITEMAP_WORKERS: ${{ vars.SECOND_PASS_SITEMAP_WORKERS || '6' }}
          PAGE_WORKERS: ${{ vars.PAGE_WORKERS || '24' }}
        run: python -u scripts/monitor_vdl.py

      - name: Build dashboard data
        run: python scripts/build_dashboard_data.py

      - name: Commit updated state and dashboard data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data public/data
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "chore: update VDL monitor state"
          git push
